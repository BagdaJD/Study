# ЕГЭ-24

- **Типовое задание(сколько определенных символов)**
    
    ```python
    ''' 11)В текстовом файле k7-53.txt находится цепочка из символов
    латинского алфавита A, B, C. Найдите длину самой длинной подцепочки,
    состоящей из символов C.
    '''
    with open('k7-53.txt') as txt:
        s = txt.read()
    
    t = 'C'
    while t in s :
        t += 'C'
    
    print(t.count('C')-1)#14
    '''
    Алгоритм:
    1)Считываем текст
    *//with open('k7-53.txt') as txt:
    //    s = txt.read()*
    2)Заводим переменную(строку) с искомым символом 
    и в дальнейшем прибавляем этот же символ
    *//t = 'C'
    //while t in s :
    //    t += 'C'*
    3)Выводим их кол-во
    *//print(t.count('C')-1)#14*
    
    '''
    ```
    
- **Задание ,где нужно считать цепочку символов**
    
    ```python
    ''' 136)  (Е. Джобс) Текстовый файл 24-J5.txt состоит не более чем из
    106 символов S, T, O, C, K. Сколько раз встречается комбинация «OCK», не
    являющаяся при этом частью комбинации «STOCK».
    '''
    
    with open('24-j5.txt') as txt :
        s = txt.read()
    
    s = s.replace('STOCK','x')
    print(s.count('OCK'))#7626
    
    '''
    
    '''
    ```
    
- **Типовое задание(через curlen)**
    
    ```python
    ''' 11)  В текстовом файле k7-53.txt находится цепочка из символов
    латинского алфавита A, B, C. Найдите длину самой длинной подцепочки,
    состоящей из символов C. '''
    
    with open('k7-53.txt') as ff:
        s = ff.read()
    
    curlen = 0
    maxlen = 0
    for i in range(0,len(s)):
        if s[i] == 'C':
            curlen += 1
            maxlen = max(curlen,maxlen)
        else:
            curlen = 0
    
    print(maxlen)#14
    '''
    Алгоритм - 
    1)Читаем файл
    2)Создаем переменные - максимальная(maxlen) длина подстроки,
    текущая длина подстроки(curlen)
    3)Создаем цикл ,где проверяем соответсвует ли наш символ
    ,указанному символу по условию(если да ,то curlen += 1,
    если нет ,то обнуляем)
    4)Находим максимальную длину(maxlen = max(curlen,maxlen))
    
    '''
    ```
    
- **Задание ,где нужно найти длину подстроки из любых повторяющихся символов(curlen)**
    
    ```python
    ''' 52)  В текстовом файле k8-0.txt находится цепочка из символов,в
    которую могут входить заглавные буквы латинского алфавита A…Z и
    десятичные цифры.
    
    Найдите длину самой длинной подцепочки, состоящей из
    одинаковых символов.
    
    Если в файл несколько цепочек одинаковой длины,
    нужно взять первую из них. Выведите сначала символ, из которого строится
    эта подцепочка, а затем через пробел – длину этой подцепочки. '''
    
    #Если нам говориться найти максимальную подстроку в строке
    #без разницы из каких одинаковых символов она будет состоять,то maxlen=curlen=1
    with open('k8-0.txt') as ff:
        s = ff.read()
    
    s = 'FFFAAAAGGVVVOOO'
    
    maxlen = 1
    curlen = 1
    
    symb = s[0]
    for i in range(0,len(s)-1):
        if s[i] == s[i+1]:
            curlen += 1
            if curlen > maxlen:
                maxlen = curlen
                symb = s[i]
        else:
            curlen = 1
    
    print(symb,maxlen)
    '''
    Если нужно найти последнюю такую подстроку ,то -
    
    if curlen >= maxlen:
            maxlen = curlen
            symb = s[i]
    
    '''
    '''
    Алгоритм - 
    1)Всё тоже самое ,только нам сказали искать длины,всех
    подстрок ,состоящих из одинаковых символов
    1.2)Поэтому maxlen=curlen=1 (читаем выше)
    2)Также надо найти символ,из которых состоит максимальная
    подстрока(symb = s[i])
    '''
    ```
    
- **Задание,где нужно вывести все максимальные длины  подстрок и их буквы**
    
    ```python
    ''' 72)  В текстовом файле k8-0.txt находится цепочка из символов, в
    которую могут входить заглавные буквы латинского алфавита A…Z и
    десятичные цифры.
    
    Найдите длину самой длинной подцепочки, состоящей из
    одинаковых символов.
    
    Для каждой цепочки максимальной длины выведите в
    отдельной строке сначала символ, из которого строится эта цепочка, а
    затем через пробел – длину этой цепочки. '''
    with open('k8-0.txt') as ff:
            s = ff.read()
    
    maxlen = 1
    curlen = 1
    
    symbStr = s[0]
    for i in range(0,len(s)-1):
        if s[i] == s[i+1]:
            curlen += 1
            if curlen > maxlen:
                maxlen = curlen
                symbStr = s[i]
            elif curlen == maxlen:
                symbStr += s[i]
        else:
            curlen = 1
    
    for symb in symbStr:
        print(symb,maxlen)
    '''
    Алгоритм - 
    1)Всё тоже самое ,только нужно будет создать массив для букв
    2)Собственно,потом делаем цикл по symbStr
    for symb in symbStr:
        print(symb,maxlen)
    
    '''
    ```
    
- **Определить длину строки ,где встречается определенное кол-во каких-либо тройек или букв**
    
    ```python
    ''' 263)  (Е. Джобс) Текстовый файл 24-263.txt состоит не более чем из
    106 символов и содержит только заглавные буквы латинского алфавита.
    Определите минимальную длину подстроки, в которой ровно три тройки BAD
    или FAT. Например, в строке SDFATFDBADZZSFATBADGHTBAD есть три
    подходящие подстроки FATFDBADZZSFAT, BADZZSFATBAD и FATBADGHTBAD.
    Минимальная длина 12. '''
    with open('24-263.txt') as ff:
        s = ff.read()
        #s = s.split('FAT')
        s = s.split('BAD')
    k = 3
    t1 = []
    #t2 = []
    for i in range(0, len(s) - k):
        w = 'BAD'.join(s[i:i+k+1])
        t1 += [len(w)]
        #w = 'BAD'.join(s2[i:i+k+1])
        #t2 += [len(w)]
    print(min(t1))
    ```
    
- **Даётся строка и нужно определить самое большое число(в 20-ой и т.д системах)**
    
    ```python
    ''' 268)  (В. Шубинкин) Текстовый файл 24-268.txt состоит не более чем
    из 106 символов и содержит только заглавные буквы латинского алфавита и
    цифры. В файле записаны числа в двадцатеричной системе счисления,
    окружённые символами, не являющимися цифрами в этой системе счисления
    или началом/концом файла. Лидирующие нули в записи чисел не допускаются.
    Определите самое большое чётное число в этом файле. Например, в
    последовательности символов FF2FTZBBC8R420Y0CCCE содержится 3 числа в
    двадцатеричной системе счисления: FF2F, BBC8 и 420. Самое большое чётное
    число – BBC8. Число CCCE не учитывается, так как перед ним стоит ноль.
    '''
    
    with open('24-268.txt') as ff:
        s = ff.read()
    
    t = []
    abc = '0123456789ABCDEFGHIJ'
    curstr = ''
    for i in (0, len(s)):
        if s[i] in abc:
            curstr += s[i]
        else:
            if curstr != '' and int(curstr,20) % 2 == 0 \
               and curstr[0] !='0':
                t +=[curstr]
            curstr = ''
    mx = 0
    max20 = ''
    for x in t:
        if int(x, 20) > mx:
            mx = int(x)
            max20 = x
    print(max20)
    ```
    
- **Второй способ решения**
    
    ```python
    '''
    267)	(ЕГЭ-2023) Текстовый файл 24-264.txt состоит
    не более чем из 106 символов и содержит только заглавные
    буквы латинского алфавита и цифры. Определите максимальную
    длину подстроки, которая может являться
    записью числа в шестнадцатеричной системе счисления.
    '''
    
    with open('24-264.txt') as ff:
        s = ff.readline()
    
    abc = sorted('0123456789QWERTYUIOPASDFGHJKLZXCVBNM')
    abc16 = abc[:16]
    bad = abc[16:]
    
    for c in bad:
        s = s.replace(c, '!')
    
    a = s.split('!')
    len_ = [len(x) for x in a if x != '']
    print(max(len_))
    ```