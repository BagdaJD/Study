```kotlin
val first30 = employees.take(30) //берет первые тридцать элементов из коллекции
val first30 = employees.takeLast(30)//берет последние тридцать элементов из коллекции

val first30 = employees.drop(30) //выбрасывает первые тридцать элементов и выводит все остальные
val first30 = employees.dropLast(30) //выбрасывает последние тридцать элементов и выводит все остальные

val array = generateSequence(0) {it + 2}//генерация последовательности, начиная с 0 с шагом 2
//т.к лямбда-выражение последний параметр, то можно без круглых скобок

//по умолчанию в последовательности находится 100 чисел
--------------
val array = generateSequence(0) {  
    println("Generated: ${it+2}")  
    it + 2  
}  
val evenList = array.drop(10)
//на самом деле ничего не проинициализируется, это называется ленивой инициализацией
//элементы последовательности будут инициализироваться <=> будут задействованы в программе
--------------
val array = generateSequence('A') {  
    println("Generated: ${it+2}")  
    it + 2  
}  
val evenList = array.take(10)  
for(i: Char in evenList){  
    println(i)  
}
//можно также создать последовательность из символов
--------------
val array = generateSequence{  
    (Math.random() * 100).toInt()  
}
//Существует перегрузка метода, в которой принимается только один параметр в виде анонимной функции
--------------
val array = generateSequence("Worker №1"){  
    val index = it.substring(8).toInt()  
    "Worker №${index + 1}"  
  
}
//Вот так еще можно делать)
//В конце всегда должно быть выводящийся результат
```