# ЕГЭ-23

- **Типовые задания**
    
    ```python
    '''
    1)  У исполнителя Калькулятор две команды, которым присвоены номера:
    1. прибавь 1
    +1
    2. умножь на 2
    *2
    Сколько есть программ, которые число 1 преобразуют в число 16?
    '''
    
    '''
    Доп тема,если старт больше финиша ,то в
    первом условии знак меняем на обратный 
    if st < fin:
    
    '''
    #1
    def f(st,fin):
        if st > fin:
            return 0
        if st == fin:
            return 1
        return f(st+1,fin) + f(st*2,fin)
    
    print(f(1,6))
    #2
    #1 -> 3 ->
    #print(f(1,3) * f(3,6))
    
    #3
    #1 ->!4 -> 6
    #if st > fin or st == 4:
    '''
    Алгоритм -
    1)Объявляем старт и финиш(из какой цифры в какую превратить)
    //def f(st,fin):
    2)Потом создаем условие,если старт перевалил за финиш возвращаем 0,
    т.к неудача,если старт == финиш,то одна программка есть
    3)И под конец просто воссоздаем все алгоритмы перехода из 1 в 16 и
    складываем их
    //т.к у нас есть команды +1 и *2 ,то используем их с st
    //return f(st+1,fin) + f(st*2,fin)
    '''
    
    '''
    Если наше исходное число больше чем конечное ,то -
    if st < fin
    '''
    ```