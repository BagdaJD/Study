# Справочник по JS

- **Введение в Js**
    
    ## Виды подключения Js
    
    ```html
    <script src="путь к js файлу">  .
    //Подключение файла//
    ```
    
    ## Объявление переменных
    
    ```jsx
    var one = 1; - обычная переменная
    let second = 2; - "ускоренная" переменная
    const pi = 3.14; - переменная которая почти 
                              не меняется
    console.log(название переменной); - вывод переменной 
    ```
    
    ## Классификация         данных
    
    ### Типы данных
    
    1. Числа 
    2. Строки
    3. Символ
    4. null(когда элемента в коде нету)
    5. undefined(это когда этот элемент есть в коде ,но без значения)
    6. объект
    7. правда - ложь
    
    ```jsx
    var number = 5; - число
    var string = "string"; - строка
    var symbol = Symbol(); - символ
    var boolean = true; - правда,ложь
    null; 
    undefined;
    var obj = { - объект
        name: "Michael",
        age: 18,
        isMarried: false
    };
    ```
    
    В Js счёт в массиве начинается с нуля 
    
    ```jsx
                            
                    0           1               2   
    let arr = ['plun.png', 'arrange.png', 'app.png']; - 
                              сам массив
    ```
    
    Пример вывода из массива значения
    
    ```jsx
    let arr = ['plun.png', 'arrange.png', 'app.png'];
    
    console.log(arr[0(номер элемента)]);  
    ```
    
    # **Типы данных JS**
    
                          **Простые типы** 
    
    1. Числа 1,2,3
    2. Строки "hello"
    3. Правда - ложь - true, false
    4. null
    5. undefined
    6. Symbol
    
                           **Объекты**
    
                       *Спец.объекты*
    
    1. Массивы - [ ]
    2. Функции - function
    3. Объект даты
    4. Регулярные выражения
    5. Ошибки
    
                  **Простые объекты**
    
- **Общение с пользователем и операторы**
    
    ## **Общение с пользователем**
    
    ```jsx
    
          (Любое сообщение)
      alert('Hello World');
      alert - вывод сообщения в браузер
    
      confirm('Ты тута?'); 
      confirm - вывод сообщения в браузер с вариантоми ответа "Да" или "Отмена"
      --------------------------------------------------------------------------
      let answer =  confirm('Ты тута?');
      console.log(answer); - в консоли при ответа "Да" будет "true" ,
      а при ответе "Отмена" будет "false"
    
                 (сообщение)       (Предполагаемый вариант ответа)
     prompt("У вас было др в этом году", "Да");
     prompt - вывод сообщения в браузер с письменным вариантом ответа
    *(Сам можешь его написать)*
    --------------------------------------------------------------------------
    let answer = prompt("У вас было др в этом году", "Да");
    console.log(answer); - в консоль выведется ответ пользователя
    
    ***(              (название переменной)
      console.log(typeof(answer));
      typeof - показывает тип переменной(string,number и т.д.)
    )***
    ```
    
    ## Операторы в Js
    
    ```jsx
        Лайфхак - чтобы перевсти строку(ответ) в число , 
        нужно привыводе на экран перед promt поставить плюс.
    (Будет если в ответе слова ,то всё равно они будут по типу данных как число)
                Пример:
    let answer = +prompt("У вас было др в этом году", "Да");
     console.log(answer);
     console.log(typeof(answer));
    -----------------------------------------------------------------------------
    Ещё один лайфхак.Чтобы сразу несколько создать переменны не обязательно 
    всегда писать let,можно просто после первой переменной поставить запятую.
    Но в конце их объявлений нужно поставить точку с запятой.
                              Пример:
    let incr = 10,
        decr = 10,
        recd = 11;
    ```
    
    ```jsx
    incr++; - инкримент(увилечение на 1 еденицу(было 10 стало 11)
    Вместо ***incr*** можно подставить название любой переменной))
    Также можно сделать так - 
    **console.log(++incr);**
    
    ------------------------------------------------------------------
    decr--; - дикримент(уменьшение на 1 еденицу(было 10 стало 9)
    Вместо ***decr*** можно подставить название любой переменной))
    Также можно сделать так - 
    **console.log(--decr);
    
                        Это было префиксная форма**
    ```
    
    ```jsx
     console.log(incr++);- возвращает к старому значению 
     и только потом прибавляет единицу.
     console.log(decr--);- возвращает к старуму значению 
     и только потом уменьшает на единицу.
    ---------------------------------------------------------
    console.log(9%4);- выводит в консоль остаток при делении
    ---------------------------------------------------------
    = - оператор присваивания(let incr = 10 и т.д.)
    == - проверка на равенство( **console.log("5" == 5); и т.д.**)
    === - сравнение по типу данных( console.log("5" === 5); - в консоль выведет false,
              console.log("5" === "5"); - будет true)
    -----------------------------------------------------------
    && - выводит только верные утверждения
    Например:
    let isChecked = true,
        isClose = true;
    
    console.log(isChecked && isClose); - выведет в консоль true.
    А если -
    let isChecked = true,
        isClose = false;
    
    console.log(isChecked && isClose); - выведет в консоль false
    ------------------------------------------------------------
    || -выводит в консоль true если одно из утверждений верно
    Например:
    let isChecked = true,
        isClose = false;
    
    console.log(isChecked || isClose); - true
    -------------------------------------------------------------
    ! - меняет true на false и наоборот
    let isChecked = false,
        isClose = false;
    
    console.log(isChecked || !isClose); - будет true
    ```
    
- **GIT**
    
    ## Git
    
    ```
    Для того ,чтобы подключить к проекту Git нужно - 
    в терминале провести путь к файлу
    
    cd C:\Users\Bagda\Documents\Занятие Js
    
    И ввести такую команду - 
    C:\Users\Bagda\Documents\Занятие Js git init
    
    ```
    
    Можно настроит еаш репозиторий Git ,как локальный или глобальный.Если локальный -  то в дальнейшем мы можем вносить в нём изменения.А если глобальный - то мы не сможем что-либо в нём менять.
    
    ```
    git config --local user.name "Bagdasar" - для того ,чтобы 
    дать название владельцу репозитория.
    
    git config --local user.email bagdasar.aydinyan2006@gmail.com 
    - для того ,чтобы подключить емайл.
    ```
    
    ```jsx
    C:\Users\Bagda\Documents\Занятие JS>git status - проверка контрольных точек у Git
    (статус)
    C:\Users\Bagda\Documents\Занятие JS>git add -A(All) - чтобы Git начал следить за файлами
    Можно вместо -A написать название(-я) файла 
    ```
    
    ```jsx
                  ( её название )
    git commit -a -m"first commit" - создание контрольной точки
    git log - просмотр всех контрольных точек
    Чтобы сделать резервную копию контр. точек нужно её загрузить на github
    (желательно с помощью второго способа)
    git remote add origin https://github.com/bagda2006/bagda_project.git
     - нужно её вставить
    git push -u origin main - запушить в репозиторий
    Если ты добавил после эотого еще файл проет , то 
     нужно с помощью git add -A добавить его и с помощью git push залить его на Github
    ```
    
    ```jsx
    (команда ,стобы клонировать репозиторий) (ссылка на него)  (название папки,которая создатся с репозиторием)    
        |                                            |           |         
    git clone https://github.com/bagda2006/bagda_project.git project_2
    git pull - для загрузки изменений другим членом команды или просто...
    ```
    
- **Условие и Циклы**
    
    ## Условия
    
    ```jsx
    Пример условия:
       (выражение)
    if (2*3 == 8) {
        console.log("Верно")
    } else {(и это ,что будет если не верно)
       console.log("Неверно")
    }
    ------------------------------------------------------------
    Поэтапная функция:
    let num = 50;
    
    if (num<49) {(если это утверждение не сработает(неверно) ,то
      console.log("Неверно")
    }else if (num>100) {(сработает это условие 
      console.log("Многовато")
    }else  {(а если и это неверно ,то сработает это)
      console.log("Верно")
    }
    
    ------------------------------------------------------------
    Условие с помощью switch - (break - ставить обязательно)
    switch (num) {
      case num < 49:
         console.log("Неверно");
        break;
      case num > 100:
        console.log("Многовато");
        break; 
      case num > 80:
        console.log("Всё ещё много");
        break;
      case 50:(проверка на равенство этой переменной)
        console.log("Верно");
        break;
      default:
        console.log("Что-то пошло не так(");
        break;
    }
    
    -------------------------------------------------------------
    (num === 50) ? console.log("Верно") : console.log("Неверно"); - ещё один способ
    ```
    
    ## Циклы
    
    ```jsx
          (выражение)
    while (num < 55) { - пример цикла)
      console.log(num);(а это действие ,которое будет выполняться)
      num++;
    }
    ---------------------------------------------------------------------
    do {
      console.log(num);(это цикл с его проверкой(это сам цикл))
      num++;
    }
    while(num < 55)(а это его проверка)
    ----------------------------------------------------------------------
     (создание пермен.) (сам цикл)
             |       /    /
    for (let i=1; i < 8; i++){(цикл)
      console.log(i) - (его сопровождение)
    }
    ------------------------------------------------------------------------
    for (let i=1; i < 8; i++){ 
     if (i === 6) {
    (прерывани е цикла) - break   }
      console.log(i)
    }                 (цикл с условием)
    ------------------------------------------------------------------------
    for (let i=1; i < 8; i++){
      if (i === 6) {
        continue - (пропускание 6)
    }
      console.log(i)
    }(цикл с пропуcканием 6)
    ```
    
- **Методы,свойства и функции**
    
    ```jsx
    AppData.expenses[a] = b; - чтобы внести переменную в обЪект
    != - не равняется
    '' - пустая строка
    && - означает и
    a.length - кол-во букв и т.д. для вводимого значения переменной
    
    AppData.moneyPerDay = AppData.budget/30; - создание новой переменной в обЪект с значением
    alert("Ежедневный бюджет: " + AppData.moneyPerDay); - вывод строки плюс переменная
    ```
    
    ## Функции
    
    ```jsx
      (создание)   (название)  
       функции       функции   (аргументы)
           |            |           |
        function showFirstMessage(text){
          alert(text); --(использование этой переменной)
       }
    
       showFirstMessage("Hello World"); - вызов функции
    
     ----!!!Все обЪявленный переменный внутри функции не могут использоваться вне её.
            Но существующие переменныу можно изменять внутри функций!!!----
    
        
    console.log(calc(3,4)); - обращение к функции и задавание значение ее аргументам
    ------------------------------------------------------------------------
    let voop = function(){
      let one = 50;               ----function expresion(а эту нет)
      return one;
    }
    
    function returnVar () {
      let num = 50;              ---function declaration(её можно вызвать до ее обЪявления)
      return num;
    }
    ----------------------------------------------------------------------------
    function returnVar () {
      let num = 50;
      return num;
    }                         - для того ,чтобы вернуть значение переменной в глобал 
    
    let anotherNum = returnVar();
    console.log(anotherNum); - выведет 50
    ```
    
    ## Методы и свойства
    
    ```jsx
    ---------------Свойства--------------
    
    length - узнает кол-во букв в выводимом слове
    console.log(str.lenght);- выясняем кол-во букв в выводимом слове,
    а их число выводится в консоль
    
    ---------------Методы-----------------
    toUpperCase - делает все буквы в слове с большой буквы
    toLowerCase - делает все буквы в слове с маленькой буквы
    
    console.log(str.toUpperCase()); 
    console.log(str.toLowerCase());
    
    Math.round(переменная) - округление числа
    
    console.log(Math.round(twelve)); = 12
    
    parseInt - перевод в другой систему счисления с округлением
    parseFloat - перевод в другую систему счисления без округления
    
    let twelve = "12.2зч";
    
    console.log(parseInt(twelve)); - 12
    console.log(parseFloat(twelve)); - 12.2
    -----------------------------------------------------------
    replace - заменяет написанной встроке или собщение 
    
    let pass = prompt('Введите пароль');
    
                      отвечает за то,чтобы 
                 все буквы поменялись
                               |
    console.log(pass.replace(/./g, "*")); - все буквы ,которые были в 
    собщении поменяютя на *          
    ```
    
- **Практика**
    
    ## Практика 2
    
    ```jsx
    isNan - команда проверяющая переиенную на содержание букв и она будет true,
    если в переменной будут буквы
    isNaN(money)
    let money, time; - объявили их для того ,чтобы они были глобальными
    
    function start() {
         money = +prompt("Ваш бюджет на месяц?");
         time = prompt("Введите дату в формате YYYY-MM-DD");
    }
    
    чтобы вызвать функцию нужно написать её название - 
    start();
    
    toFixed - округление чисел
    
    AppData.moneyPerDay = (AppData.budget/30).toFixed();
    toFixed(1) - округление до первого числа после запятой
    ```
    
- **CallBack-function,массивы и объекты**
    
    ## Callback-функции
    
    ```jsx
    function learnJs(lang, callback){
      lang(); -  пример вызова аргумента
    } 
    //callback-функция это функции ,которая должна заработать после того
    //,как другая функция завершилась.Тоесть callback-функция не начнётся 
    //,пока не сработает другая команда написанная перед ней
    //Также callback-функции задаёт порядок выполнений других функций
    
    function one(){
     console.log(1+2);
    }
    function two(){
     console.log(2+4);
    }
    function call(callback, golos){ - примеры callback-функции
      callback();
      golos();
    }
    call(two, one);
    ```
    
    ## Объекты
    
    ```jsx
    let options = {
        name: "text"
        object
    }
    
    options.objects = {
        color: "red" - пример добавления объекта в объект
    }
    
    console.log(options.name) - вывод элементов из объекта
    
    options.bool - создание новой переменной в обЪект
    -------------------------------------------------------------------------
    let options = {
        objects: {
          color: "red", - обЪект так же может хоанить другие обЪекты внутри себя
        },
        name: "text"
    }
    -------------------------------------------------------------------------
    delete options.name; - чтобы удалять ,что-либо в объекте
    
    for(let key in options){
        console.log(' Свойство: ' + key + " Имеет значение " + options[key]); 
    } - чтобы выяснить ,что за значение и и , что за элемет в в объекте
    
    console.log(Object.keys(options).length);  - чтобы выяснить кол-во значений в объекте
    ```
    
    ## Массивы
    
    ```jsx
    let arr = [1, 2, 3, 4]; - пример массива
    
    (название)
        |
    arr.***pop***(); - чтобы удалить последний элемент в массиве
    
    (название)
        |
    arr.***push***('7'); - чтобы добавить в конец массива элемент
    
    (название)
        |
    arr.***shift***(); - чтобы удалить первый элемент в массиве
    
    (название)
        |
    arr.***unshif***t("1"); - чтобы добавить в начало массива элемент
    -------------------------------------------------------------
    for(let i = 0; i<arr.***length***; i++){
        console.log(arr[i]); - чтобы перырыть весь массив
    }
    
    arr.***forEach***(function(item, i, mass){
        console.log(i + " : " + item + " массив: " + mass ); - также чтобы
                                                            перырыть весь массив 
    })
    
    slides.forEach((item) => item.style.display = 'none'); - использование с стрел.функциями
    dots.forEach((item) => item.classList.remove('dot-active'));
    
    arr[99] = 99; - добаввление элементов с их значениями в массив
    
    for(let key in mass){
        console.log(key); - получение кол-во элементов
    }
    
    for(let key of mass){
        console.log(key); - получение значений
    }
    -------------------------------------------------------------
    let ans = prompt("", ""),
        arr = [];
    
    arr = ans.***split***(','); - в этот массив будут записываться данные ,
    которые были введены через запятую как отдельные данные,а не как единые и 
    будут являться строкой по типу данных
    
    let arr = ["name","password","login"];
         i = arr.***join***(); - здесь запишутся все данные в одну строку
    console.log(i);
    
    let arr = ["name","password","login"];
       
    i = arr.***sort***(); - все элементы будут сортироваться по алфавиту
    console.log(arr);
    
    let arr = [1,2,13,4];
    
    function compareNum(a,b){
        return a-b;
    }                        -  для того,чтобы цифры сортировались по порядку
    
    let i = arr.***sort***(compareNum);
    console.log(arr);
    
    let arr = ['name', 'password', 'email'];
       console.log(arr.splice()); - удаление содержимого массива или по параметрам
    
    let arr = ['name', 'password', 'email'];
       console.log(arr.slice()); - копирование массива
    
    let arr = ['name', 'password', 'email'];
       console.log(arr.reverse()); - меняет порядок массива на обратный
    
    let arr = ['name', 'password', 'email'];
       console.log(arr.concat(mass)); - создание массива внутри мкассива
    ```
    
- **ООП**
    
    ## ООП
    
    ```jsx
    let solder = {
       heath: 300,
       arm: 50
    }
    
    let Nichol = {
        heath: 100
    }
    
    Nichol.__proto__=solder; - это показывает, то ,что Nichol это наследник 
    прототипа solder
    
    console.log(Nichol); - покажет ,что это объект и выведет его данные
    console.log(Nichol.arm); - покажет arm у прототипа solder
    ```
    
- **Динамическая типизация в JS,получение информации со страницы,действия с элементами на странице**
    
    ## Динамическая типизация в Javascipt
    
    ```jsx
    //Способы превращения любого типа данных в строку
    
    console.log("www" + 6); - получится строка(и т.д.)
    String(7) - также получится строка
    
    //Способы превращения любого типа данных в число
    console.log(+"6"); - получится число(и т.д.)
    console.log(typeof(5 + +"5"));- пример
    console.log(parseInt('15px')+10); - пример ,можно также через parseInt
    
    Number("8") - также получится число
    
    //Способы превращения любого типа данных в boolean(true/false)
    console.log(Boolean("7")); - получится boolean
    console.log(!!'5'); - получится boolean
    
    let food = null;
    
    if(food){ - не будет работать пока food false
    console.log("working");
    }
    
    food = 1; - с помощью передачи значений
    
    if(food){ - заработает потому что food true то есть есть значение
    console.log("working");
    }
    
    //0,'',null - false
    ```
    
    ```jsx
    //Разбор заданий
    let x = 5;
    console.log(x++); - выведется 5 ,но если потом вызвать эту переменну ,то будет 6
    --------------------------------------------------------------------------------
    console.log([] + false) - будет false
    console.log([] + false + null) - будет Nan
    //[]- пустой массив всегда равен строке
    --------------------------------------------------------------------------------
    let y = 1,
     x = y = 2; - такие выражения нужно читать справа-налево
    console.log(x)
    --------------------------------------------------------------------------------
    console.log([] + 1 + 2) - бует "12" , т.к. [] пустой и он будет являться строкой,
    а всё что с трокой слаживается будет строка
    --------------------------------------------------------------------------------
    console.log("25"[1]); - выведется '5' т.к. отчёт в массиве начинается с 0, 
    а второй цифрой будет являться 5
    --------------------------------------------------------------------------------
    console.log(2 && 1 && null && 0 && undefined); - выведется null
    console.log(null || 2 && 3 || 4); - выведется 3 так как выводится всегда правада 
    в таких выражениях
    
    console.log(0 || "" || 2 || undefined  || true || false) - выведется 2 ,
    так как здесь оно единственный правдивый элемент
    ```
    
    ## Получение инфы со страницы
    
    ```jsx
    DOM - объектная модель документа
    
    let btn = document.getElementById('btn'); - получение инфы с html через id
    let all = document.getElementsByTagName('button'); - получение инфы с html через н
    азвание тега
    let text = document.querySelectorAll('.txt'); - получение инфы с html через 
    класс элемента
    let silka = document.querySelector('.silka'); -  получение инфы с html через 
    класс элемента,но мы получим только первый элемент с таким классом
    ```
    
    ## Действия с элементами на странице
    
    ```jsx
     название переменной   Css свой-во
     с элементом           которое хотим поменять
          |                   |
         btn.style.backgroundColor = 'blue'; - значение на которое его меняем
         
    document.body.style.backgroundImage = "url('img/apple_true.jpg')"; - чтобы поменять 
    картинку на заднем фоне(просто путь другой провести)
     
         newBtn[1].style.borderRadius = '100%'; - чтобы изменить определенный 
          элемент из списка
    
    let div = document.createElement("div"); - создание html тега
    let bublik = document.createTextNode("Тут был Паша"); - создание надписи
    div.classList.add('black'); - чтобы добавить класс к элементу
    
    document.body.appendChild(div); - добавление переменной в конец страницы
    
    document.body.insertBefore(div, btn); - добавление элемента 
    перед каким-либо другим элементом
    
    adv.remove(); - чтобы просто удалить элемент
    
    document.body.removeChild(text[1]); - чтобы удалить вложенный элемент 
    
    wrapper.removeChild(newBtn[0]); - удаляет элемент 
    ,который находится в другом элементе
    
    document.body.replaceChild(text[0], silka[1]); - чтобы один удалить элемент,
    а другой вместо него добавить
    
    div.innerHTML = 'Hello world'; - вставление текста в элемент
    div.innerHTML = '<h1>Hello world</h1>'; - вставление html тега в элемент
    div.textContent = 'Hello world'; - вставление текста в элемент
    (также можно с помощью этого метода заменить текст в элементе)
    
    let opinionForApple = prompt('Как вы относитесь к технике Apple?');
    promptForApple.textContent = opinionForApple; - чтобы записалось мнение в promptForApple
    с помощью prompt
    
    //Лайфхак
    //Что выбрать элемент с одинаковым классом или тегом можно сделать так - 
     optionalExpensesBtn = document.getElementsByTagName('button')[0] - и так мы 
       выберем первый элемент с таким классом или тегом
    ```
    
- **События,события на мобильных приложениях,регулярные выражения**
    
    ## События
    
    ```jsx
    btn[0].onclick = function(){
        alert('Артём додик'); - при клике выводится Артём додик
    }
            
                           когда произойдёт
                               |
    btn[0].addEventListener('click', function(){
        alert('Артём додик');           \
        alert('Артём додик2');         что произойдёт
    })
    
    //click - будет происходить при клике
    //mouseenter - будет происходить при наведении
    
    btn[0].addEventListener('mouseenter', function(){
        alert('вы навелись на катяха!Осторожно!'); - пример
    })
                            указывает на то, что здесь было событие
                                                |
    btn[0].addEventListener('click', function(event){
        console.log('произошло событие: ' + event.type + 'на элементе' + event.target);
    })                                        |                               |
                                      чтобы узнать тип события        на каком элементе
                                                                      произошло событие
    
    btn[0].addEventListener('click', function(event){
        let target = event.target; - использование аргумента ивент
        target.style.display = 'none';
    })
    
    ***//Всплытие событий происходит сначала на самом вложенном элементе,
    //потом на родителе и выше ,и выше.***
    
    event.preventDefault(); - чтобы событие прошло ,но ничего не произошло на странице
    
    silka.addEventListener("click", function (event) {
      event.preventDefault(); - с помощью этого метода мы по силке никуда не перейдём
      console.log(
        "произошло событие: " + event.type + "на элементе" + event.target
      );
    });
    
    btn.forEach(function(item){
       item.addEventListener('mouseleave', function(){
           console.log('Мы вышли'); - чтобы назначить события всем элементам
                                       в псевдомассиве с помощью forEach
       })
    })
    
    //yearValue.value = '28'; - чтобы вставить ,что-нибудь в input
    
    yearValue.value = new Date(Date.parse(time)).getFullYear();
    - чтобы выделить год из даты
    monthValue.value = new Date(Date.parse(time)).getMonth() + 1;
     - чтобы выделить месяц  из даты
    dayValue.value = new Date(Date.parse(time)).getDate();
     - чтобы выделить день из даты
    
    //sum += +b; - чтобы сложить переменную с собой
    
    abc.addEventListener("input", function({}) - используется только с инпутами.
    Когда мы что в инпут вписываем, он сразу отображается(например или 
    что-то другое происходит).
    
    abc.addEventListener("input", function({}) - используется только с инпутами.
    Когда мы что в инпут вписываем, он отобразится только при клике на что-то другое
    (например или что-то другое происходит).
    
    ```
    
    ## События на мобильных приложениях
    
    ```jsx
    touchstart - аналог клика ,но только для телефонов
    ----------------------------------------------------------------
    touchmove - когда пользователь удерживает пльцем элемент(двигает)
    ----------------------------------------------------------------
    touchend - когда пользователь уже убирает палец от элемента
    ----------------------------------------------------------------
    touchenter - когда пользователь двигал палец и зашёл намерено на другой элемент
    ----------------------------------------------------------------
    touchleave - когда элемент двигал палец и вышел намеренно из элемента
    ----------------------------------------------------------------
    touchcancel - когда палец уже не регистрируется на экране
    
    event.preventDefault() - запрещает действовать браузеру по умолчанию
    с выбранным элементом
    
    touches - считает кол-во пальцев зарегистрировано на дисплее
    console.log(event.touches); - выведет кол-во пальцев
    
    changedTouches - показывает список пальцев ,которые взаимодействовали с экраном
    console.log(event.changedTouches); 
    
    targetTouches - регистрирует нажатия на элементы с этим методом
    console.log(event.targetTouches);
    
    target - показывает элементы на которые нажали
    console.log(event.target);
    
    console.log(event.touches[0].target); - чтобы зарегать палец на экране 
    и куда он нажал
    ```
    
    ## Регулярные выражения
    
    ```jsx
    Регулярные выражения нужны для работы с строками
    
     new RegExp('patter', 'flags'); - патерн
     /pattern/ - патерн
    
    let ans = prompt('Введите имя');
     
    let reg = /n/; - патерн 
    
    console.log(ans.search(reg)); - будет искать регулярное выражение
    В итоге в консоль выведется позиция первой найденной буквы
    
                                     **Флаги
    i - флаг регистра(большая или малнькая буква)
    g- флаг глобальности ,который ищет глобально
    m - флаг многосторочности**
    
    let ans = prompt('Введите имя');
     
    let reg = /n/g; - патерн 
    
    console.log(ans.match(reg)); - выведет в консоль  массив с повторениями n
    
    //let reg = /n/gi - можно также одновременно использовать несколько флагов
    
    let pass = prompt('Введите пароль');
    
     console.log(pass.replace(/./g, "*")); - все буквы поменяются на * - регулярные выражения
     alert('12-34-56'.replace(/-/g, ':')); - все - поменяются :  ------ /
    --------------------------------------------------------------------
    let ans = prompt('Введите имя');
    
    let reg = /n/gi;
    
    console.log(reg.test(ans)); - проверит регулярное высказывание на нахождение *n
    
                                        Классы
    \d - чтобы искать цифру с помощью регулярного выражения
    \w - чтобы искать букву с помощью регулярного выражения
    \s - чтобы искать пробелы с помощью регулярного выражения
    \D - чтобы не искать цифру с помощью регулярного выражения
    \W - чтобы не искать букву с помощью регулярного выражения
    \S - чтобы не искать пробелы с помощью регулярного выражения
    
    let reg = /\d/g;   - поиск всех цифр(анологично с другими классами)
    console.log(ans.match(reg));
    
    console.log(str.match(/\w\d\w\d/i)); - пример сложного регулярного выражения
    //console.log(str.match(/\//i)); - чтобы найти слеш в сообщении( и т.д 
    //с другими элементами например *, , . '' и т.д.)*
    ```
    
- **Скрипты и время их выполнения**
    
    ## Скрипты и время их выполнения
    
    ```jsx
    setTimeout() - задаёт время и ,что будет происходить спустя этого время
                
                     функция отвечающая ,за 
                      то ,что произойдёт
                                 |             время за которое 
                                 |        что-либо произойдёт
                                 |         /                         
    let timerId = setTimeout(sayHello, 3000);
    clearTimeout(timerId); - делает так ,чтобы ничего не произошло
    
    function sayHello(){
      alert('Hello world');
    }
    ---------------------------------------------------------------------------------
    setInterval() - задаёт время и ,что будет происходить постояно спустя этого время
    
    let timerId = setInterval(sayHello, 3000);
    clearInterval(timerId); - тоже самое
     
    function sayHello(){
      console.log('Hello world');
    }
    ---------------------------------------------------------------------------------
    let timerId = setTimeout(function log(){
      console.log('Hello');
      setTimeout(log, 2000); - рекурсивный setTimeout
    })
    ***Рекурсивный setTimeout*** - ***это setTimeout ,который вызывает сам себя.
    И сохраняет интервал между выполняемыми действиями.Т.к. setInterval может продлевать 
    этот интервал выполнения действия.***
    ---------------------------------------------------------------------------------
    ***btnBlock.addEventListener('click', function(event){
        if(event.target && event.target.tagName == 'BUTTON'){
            console.log('ok'); - чтобы события назначались на все кнопки
        }
    })
    
    Делегирование - это переносить ,что-либо с родителя к внутреннеиу элементу***
    
    ---------------------------------------------------------------------------------
    ***btnBlock.addEventListener('click', function(event){
        if(event.target && event.target.classList.contains('first')){
            console.log('ok'); - чтобы события назначались на все кнопки c таким классом
        }
    })***
    ---------------------------------------------------------------------------------
    ***btnBlock.addEventListener('click', function(event){
        if(event.target && event.target.classList.matches('button.first')){
            console.log('ok'); - чтобы события назначались только на первую кнопку с 
                                            таким классом
        }
    })***
    ```
    
- **Параметры документа**
    
    ## Создание табов на странице
    
    ```jsx
    window.addEventListener('DOMContentLoaded', function(){
            'use strict';
    }) - чтобы скрипт js запускаллся только тогда, 
    когда уже всё загрузилось и было на своих местах
    
    'use strict'; - чтобы перевести весь код в строгий режим
    
    if(tabContent[b].classList.contains('hide')) - проверка на имение класса
    ```
    
    ## Параметры документа
    
    ```jsx
    let box = document.querySelector('.box'),
         boxWidth = box.clientWidth, - получение ширины элемента
         boxHeight = box.clientHeight; - получение высоты элемента
    
    box.offsetHeight - получение высоты элемента c учётом Css стилей
    box.offsetWidth - получение ширины элемента с учётом Css стилей
    -------------------------------------------------------------------------
    box.scrollHeight - получение высоты элемента c учётом скрола(пролистования)
    box.scrollWidth - получение ширины элемента с учётом скрола(пролистования)
    
    -------------------------------------------------------------------------
    let btn = document.querySelector('.btn');
    
    btn.addEventListener('click', function(){
        box.style.height = box.scrollHeight + 'px'; - для того ,чтобы 
                                         при нажатии на кнопку элемент раскрывался
    })
    -------------------------------------------------------------------------
    box.getBoundingClientRect() - получение координат элемента
    
    ***//Чтобы получить координаты только с одной стороны  нужно сделать так - 
    //box.getBoundingClientRect(.left(.bottom или .top или .left или .right))*** 
    
    document.documentElement.scrollTop() - показывает на сколько пикселей 
    было отмотано по странице при скролле
    
    scrollBy(x, y) - перематывает страницу по координат x и y
    scrollT0(x,y) - перематывает страницу на определённые координаты
    
    ***//Math.floor((t/1000)) - чтобы выделить целые числа после деления например
    //% - для получение остатка при делении
    
    return{
            'total' : t,
            'hours' : hours,
            'minutes' : minutes,
            'seconds' : seconds 
                        - чтобы вернуть значений переменных из функции в глоб код
        }***
    hours =  timer.querySelector('.hours'), - чтобы искать первый класс
     в другом элементе
    ```
    
- **Конструкторы и классы**
    
    ## Конструкторы и классы
    
    ```jsx
    Функции конструкторы нужны для создания одного и того же.
    Для создания новых пользователей,например.(объекты и т.д.)
    
    function User(name, id){
         this.name = name;
         this.id = id;
         this.human = true; - пример функции конструктора
    }
    let ivan = new User('Ivan', 25);
    
    let alex = new User('Alex', 20);
    
    console.log(ivan);
    console.log(alex)
    
    ***//Контекст вызова this
    //1) просто вызов функции - window/undefined
    //2) метод объекта - this = объект
    //3)конструктор(New) - this = новый созданый объект
    //4)Указание конкретного контекста - call,apply,bind
    //btn.addEventListener('click', function(){
      console.log(this); - выведеться элемент ,на который повесили событие
    })***
    
                                                (тут прям объект)
    ******console.log(sayName.call(user)); - чтобы привязать данные к функции или конструктору
    console.log(sayName.apply(user)); - чтобы привязать данные к функции или 
    конструктору(здесь просто значение)
    
    console.log(sayName.apply(user, ['Snow'])); - чтобы передать ,также еще 
    много других данных в виде массива
    
    console.log(sayName.call(user, 'Aydinyan')); - чтобы передать ,также еще 
    данные в виде строки
    
    function count(number){
      return this*number;
    }
    
    let double = count.bind(2); - возвращает указанное значение в this(this = 2)
    
    //document.body.style.overflow = 'hidden'; - чтобы убрать прокрутку
    ```
    
- **Доп.инфа конструкторы и классы**
    
    ## Классы и конструкторы
    
    ```jsx
    
    function User(name) {
      this.name = name;
      this.isAdmin = false;
    } - функция-конструктор
    
    let user = new User("Вася");
    
    alert(user.name); // Вася
    alert(user.isAdmin); // false
    
    //Имя функции-конструктора должно начинаться с большой буквы.
    //Функция-конструктор должна вызываться при помощи оператора ***"new"***.
    
                                 ***new.target***
    //Используя специальное свойство new.target внутри функции, мы можем проверить, 
    //вызвана ли функция при помощи оператора new или без него.
    //В случае, если функция вызвана при помощи new, то в new.target будет сама функция,
    //в противном случае undefined.
    
    function User() {
      alert(new.target);
    }
    
    // без "new":
    User(); // undefined
    
    // с "new":
    new User(); // function User { ... }
    
    //Обычно конструкторы ничего не возвращают явно. Их задача – записать все необходимое в this, который в итоге станет результатом.
    //Но если return всё же есть, то применяется простое правило:
        //При вызове return с объектом, будет возвращён объект, а не this.
        //При вызове return с примитивным значением, примитивное значение будет отброшено.
    //Другими словами, return с объектом возвращает объект, в любом другом случае конструктор вернёт this.
    //В примере ниже return возвращает объект вместо this:
    
    function BigUser() {
    
      this.name = "Вася";
    
      return { name: "Godzilla" };  // <-- возвращает этот объект
    }
    
    alert( new BigUser().name );  // Godzilla, получили этот объект
    
                                 ***Classes***
    class User {
    
      constructor(name) { - метод конструктор
        this.name = name;
      }
    
      sayHi() { - метод
        alert(this.name); - пример класса 
      }
    
    }
    
    // Использование:
    let user = new User("Иван");
    user.sayHi();
    
    //
    
    // "Named Class Expression"
    // (в спецификации нет такого термина, но происходящее похоже на Named Function Expression)
    let User = class MyClass {
      sayHi() {
        alert(MyClass); // имя MyClass видно только внутри класса
      }
    }; - Class Expression
       
    new User().sayHi(); // работает, выводит определение MyClass
    
    alert(MyClass); // ошибка, имя MyClass не видно за пределами класса
    ```
    
- **ES6**
    
    ## Интерполяция
    
    ```jsx
    Интерполяция-простой способ вставления переменной или выражения
    let name = "ivan",
         age = 30,
         mail = "exm@mail.ru"; - пример интерполяции                       
               
                            название переменной                   
                                     |
    document.write(`Пользователь ${name} ${age} лет.Его почта ${mail}`); - выведет:
    Пользователь ivan 30 лет.Его почта exm***@***mail.ru
    
    //https://habr.com/ru/post/305900/ - шпаргалка по ES6
    ```
    
    ## Стрелочные функции
    
    ```jsx
    1.Стрелочная функция анонимна и мы не можем дать ей имя ,и 
    назначать обработчика союытий
    2.У стрелочной функции нет своего контекста вызова.Она берёт свой контекст у 
    свокго родителя
    ```
    
    ## Параметры по умолчанию
    
    ```jsx
    
    задаём параметр по умолчанию к аргументу number если он не будет указан ,
                     то будет равен 2
                              |
    function calcOrDouble(number = 2, basis){
        console.log(number*basis);
    };
    ```
    
    ## Классы
    
    ```jsx
    class Rectangle { - класс
        constructor(height, width){
            this.height = height; - чтобы в любом экземпляре образованного
            от этого класса его ширина равнялась заданной ширине
            this.width = width;- чтобы в любом экземпляре образованного
            от этого класса его длина равнялась заданной длине
        }
        calcArea(){ - метод
            return this.height * this.width;
        }
    }
    
    const square = new Rectangle(20, 10); - вызов класса
    
    console.log(square.calcArea()); - наследование метода от класса
    
    //Название класса должно начинаться с большой буквы
    //Вызывать класс нужно с помощью new
    //Не нужно ставить запятые в классе
    ```
    
    ## Spread-оператор
    
    ```jsx
    ... - spread-оператор 
    
    let video = ["youtube", "rutube", "vimeo"],
      blogs = ["wordpress", "livejournal", "blogs"],
      internet = [...video, ...blogs, "vk", "facebook"]; - добавляет другие массивы в этот
    console.log(internet);  
    
    function log(a, b, c, d) {
      console.log(a);
      console.log(b);
      console.log(c);
      console.log(d);
      console.log(c + a + b);
    }
    
    let number = [1, 3, 5, 7];
    
    log(...number); - добавлят этот массив в использование function log
    ```
    
- **JSON,PROMISE,AJAX**
    
    ## JSON
    
    ```jsx
    let options = {
       width: 1366,
       height: 1000,
       background: 'red',
       fontSize: {
         size: '16px',
         color: 'white'
       }
    };
    
    console.log(JSON.stringify(options)); - чтобы преобразовать JS объект в JSON объект
    
    JSON.stringify(options) - преобразовывает объекты в JSON формат
    
    //В в JSON объекте все пары(ключ:значение) будут написаны с помощью ""
    
    console.log(JSON.parse(JSON.stringify(options))); - чтобы JSON объект преобразовать в 
    JS объект
    
    JSON.parse() - преобразовывает объекты в JS формат
    
    //rub.addEventListener('input', () =>{}) - это событие для input,которое при вводе инфы в
    // него обновляет инфу грубо говоря
    ```
    
    ## AJAX
    
    ```jsx
    rub.addEventListener('input', () =>{
      let request = new XMLHttpRequest(); - для того ,чтобы AJAX начал работать
    
      request.open(method, url, async, login, pass);
    })
     
    request.open() - метод XMLHttpRequest,котрый принимает данные,такие 
    как method(GET или POST),url(путь к БД), async(ассинхронность, то ,
    есть ответ сервера(время)),login и pass(логин и пароль)
    
    request.open(method, url, async, login, pass); - пример
    
    request.setRequestHeader('Content-type', 'application/json; charset=utf-8'); ----
    ---задаём параметры запроса
    
    request.send(); - информацию запроса,ну или 'тело запроса'
    
                             Св-ва XMLHttpRequest
    status - присылает состояние сервера в виде кодовой ошибки(404 например)
    statusText - присылает состояние сервера в виде текста
    responseText - ответ работы сервера в тексте(данные) похоже с statusText
    readyState - стадии работы сервера
    
                                       Лайфхак
    request.addEventListener("readystatechange", function () {
         if(request.readyState === 4 && request.status == 200){ - пример этого события
           
         }
      });
    });
    
    readystatechange - событие ,которое позволяет отслеживать работу
    сервера при поступлении запроса
    
    let data = JSON.parse(request.response);
    
      inputUsd.value = inputRub.value / data.usd; 
    
    data.usd - чтобы выбрать ключ со значением из JSON файла
    //
    formData - позволяет отправлять данные от клиента на сервер
    Пример:
    let formData = new FormData(form); - получаем данные из формы
    form.addEventListener("submit", function (event) {} - чтобы при клике на кнопку(например) 
    можно было отправить данные
    
    //        Лайфхак - как преобразовать formData в JSON
    
    let obj = {};
        formData.forEach(function (value, key) {
          obj[key] = value;
        });
        let json = JSON.stringify(obj);
    ```
    
    ## Promise
    
    ```jsx
    Promise - переводится ,как обещание.(Наподобие условий)
    
    //? - оператор тогда
    //: - оператор в противном случае
    function shoot(arrow, headshot, fail){
      console.log('Выстрел произошёл...');
    
      setTimeout(function () {
        Math.random() > .5 ? headshot() : fail();; - если > 50% ,то выполнится функция 
       headshot,а если нет ,то выполнится функция fail
      })
    }
    
    .then() - произойдёт если ,что-то верно или что-то ещё произойдёт
    .catch() - произойдёт если , .then не произойдёт
    
    shoot({})
              .then(mark => console.log('Вы попали в цель'))
                .then(win)
                 .catch(lose)
    ```
    
- **Local Storage,Try и Catch**
    
    ## Local Storage
    
    ```jsx
                         ключ    знач
                           |       |
    localStorage.setItem("number", 5); - чтобы записать ключ + значение в Local Storage
    
              ключ из которого хоти получить знач
                           |
    localStorage.getItem("number") - чтобы получить ключ + значение из Local Storage
    localStorage.removeItem("number") - чтобы удалить ключ + значение из Local Storage
    
    localStorage.clear() - чтобы удалить всё в local storage
    
    //Метод для checkbox(Лайфхак)
    //if(localStorage.getItem("isChecked") === "true"){
    //       checkbox.checked = true; - если условие будет верно ,то checkbox 
    //                                     всегда будет выбранным
    //  }
    ```
    
    ## Try и Catch
    
    ```jsx
    try и catch - нужны для того ,чтобы код при наличии ошибки не переставал работать
    
    try{
        console.log('Begin');
        console.log(a);
        console.log('Good job' + a);
    }catch (error){ - если что-то из блоока try не сработает и не будет верно 
        console.log(error);                      ,то сработает блок catch  
    }
    
                               **Св-ва try и catch
    catch (error){
        console.log(error.name) - выдаст название ошибки
    }
    
    catch (error){
        console.log(error.message) -  выдаст описание ошибки
    }
    catch (error){
        console.log(error.stack); - выдаст причину и где мы её совершили ошибки
    }
    if(!user.name){
           throw new Error("Uhyhyhyhy uou are hacked!"); - чтобы вывести текст при 
                                                            определенных ошибках
       }**
    ```
    
- **Jquery,Инкапсуляция,Модули**
    
    ## JQUERY
    
    ```jsx
    $('.btn'); - чтобы получить элемент с html-страницы с таким классом
    
    $(document).ready(function (){ - тоже самое как DOMContentLoaded
      $('.list-item:first').hover(function (){ - при наведении
        $(this).toggleClass('active'); - чтобы добавить еласс
      });
    
      $('.list-item:eq(2)').on('click', function (){ - событие(addEventListener)
         $('.image:even').fadeToggle('slow'); - анимация со скоростью
      });
    
      $('.list-item:eq(4)').on('click', function (){
        $('.image:odd').animate({ - своя анимация
          opacity: 'toggle', - параметры анимации
          height: 'toggle',
        }, 3000);
      })
    });
    ```
    
    ## Инкапсуляция
    
    ```jsx
    Геттеры и сеттеры нужны для более безопасного написания кода
    
    function User(name, age) {
      this.name = name;
      let userAge = age;
    
      this.say = function () {
        alert(`Имя пользователя ${this.name}, возврат ${this.userAge}`);
      }
    
      this.getAge = function () { - геттер
        return userAge; - делаем так,чтобы можно было 
                        использовать эту переменную
      }
    
      this.setAge = function (age) { - сеттер
        if (typeof age === 'number' && age > 0 && age < 110) {
          userAge = age; - делаем так,чтобы была
                       возможность изменить значение  userAge
          } else {
          console.log("Недопустимое значение");
        }
      }
    }
    
    let ivan = new User('Ivan', 25);
    
    console.log(ivan.name);
    console.log(ivan.userAge);
    console.log(ivan.getAge())
    
    ivan.userAge = 30;
    ivan.name = 'Misha';
    
    console.log(ivan.name);
    console.log(ivan.age);
    console.log(ivan.setAge());
    ```
    
    ## Модули
    
    ```jsx
    модули - это куски кода,независящие от остального кода(и наоборот).
    Если удалить модуль из кода ,то код не сломается
    
    //Примеры создания модулей
    
    //1
    let num = 1;
    (function (){
        let num = 2;
        console.log(num);
    
        return console.log(num + 3);
    }())
    
    console.log(num);
    //2
    let user = (function (){
        let privat = function (){
       console.log('i am privat');
        }
    
        return {
            sayHello: function (){
                console.log('hello!');
            }
        }
    
    }())
    
    console.log(user);
    console.log(user.sayHello());
    
    //3
    
    let user = (function (){
        let privat = function (){
            console.log('i am privat');
        }
    
        let sayHello = function (){
            console.log('HelO');
        }
    
        return {
            sayHello: sayHello
        }
    
    }())
    
    console.log(user);
    console.log(user.sayHello());
    ```
    
- **WebPack,BabelJS,Импорт и экспорт переменны**х
    
    ## WebPack
    
    ```jsx
                       Export и Import файлов в JS
    //my.js
    function  myModule(){
        this.hello = function (){
            return 'hello';
        }
    
        this.goodbye = function (){
            return 'Goodbye';
        }
    }
    
    module.exports = myModule; - что именно я хочу из этого кода экспортировать
    //script.js
    let myModule = require('./my'); - откуда я импортирую
    
                        Установка WebPack в проект(терминал)
    1.Для начала нужно в терминал ввести npm init
    2.npm install webpack --save-dev - ввести эту команду для установки webpack
    3.npm install webpack-cli --save-dev - ввести эту команду для
                                            установки утилиту для webpack
    
                  ***Использование* webpack**(без конфигурации)
    1.Нужно сделать две папки Src,Dist(в папке Src должны лежать файлы js,а
     в папке dist будет лежать сжатый код)
    2.Файл ,который импортирует модуль из другого кода нужно переменовать 
    в index.js(и переменные с именем module лучше не создавать)
    3.Написать команду npx webpack
    
                   ***Использование* webpack с конфигурацией
    Начало-**Тоже самое ,что и без конфигурации + плюс нужно будет весь код поделить
     на модули и сохранить их по отдельности + сделать файл с импортов этих модулей и вызвать их
    
      пример -
    window.addEventListener('DOMContentLoaded', function (){
        "use strict";
        let calc = require('./Calculator'),
            form = require('./Form'),
            modal = require('./Modal'),
            slider = require('./Slider'),
            tabs = require('./Tabs'),
            timer = require('./Timer');
    
        calc();
        form();
        modal();
        slider();
        tabs();
        timer();
    })
    2.Создаём файл webpack.config.js с таким кодом.Например - 
    
    'use strict';
    
    let path = require('path');
    
    module.exports = {
      mode: 'development', - мод(development,production,final)
      entry: './js/Parts/Connect.js',(путь к файлу ,который объединяет и другие файлы)
      output: {
        filename: 'bundle.js', - название файла ,который получится после сжатия и сбора
        path: __dirname + '/dist' - где этот файл появится ,после сжатия
      },
      watch: true,  - разрешение для webpack,чтобы 
                       он автоматически собирал файлы
    
      devtool: "source-map" - исходники
    };
    ```
    
    ## BabelJS
    
    ```jsx
                      Начало работы с babel
    1.Если в своём проекте мы использовали webpack ,то лучше скачать плагин к нему.
    Следовательно скачиваем плагин babel-loader
    npm install -D babel-loader @babel/core @babel/preset-env webpack
    2.Затем в webpack.config.js вставляем этот код - 
    
    module: {
        rules: [
          {
            test: /\.js$/,
            exclude: /(node_modules|bower_components)/,
            use: {
              loader: 'babel-loader?optional[]=runtime',
              options: {
                presets: [
                  ["@babel/env", {
                    targets: {
                      edge: "17",
                      firefox: "60",
                      chrome: "67",
                      safari: "11.1",
                      ie: "11"
                    }
                  }]
                ]
              }
            }
          }
        ]
      }
    3.Затем мы должны отдельно подключить компаненты babel(ES6-Promiseб polyfill,
    babel-plugin-es6-promise,Polyfill for IE11 missing NodeList-foreach.js,
    foreach-polyfill npm,formdata-polyfill)
    4.Затем нужно их добавить в файл со всеми модулями и желательно в начало
    
    В 3-ум пункте можно это добавить либо через полифилы , либо через плагин
    (если полифил,то его нужно скачать и подключить к файлу с модулями 
    пример - 
    require('es6-promise').polyfill();
    если через плагины ,то нужно его скачать и добавить в webpack.config.js
    Пример - 
    plugins: ["es6-promise"] - эту запись нужно вставить после после presets
    ```
    
    ## Экспорт и Импорт переменных
    
    ```jsx
    Экспорт - 
    export let one = 1;
    
    let two = 22;
    
    export {two};
    
    export function sayHi(){
        console.log('hi');
    }
    
    Импорт - 
    import {one, two} from './my';
    
    console.log(one + two);
    ```
    
- **Написание тестов**
    
    ## Написание тестов
    
    ```jsx
    Есть сервисы такие как Mocha и Chai.Они помогают написать 
    тесты для вашего приложения и тд.
    
              Начало использования
    1.Для начало их нужно скачать
    2.Создать папку test
    3.Затем нужно подключить к коду их
    
    let assert = require('chai').assert - Сhai
    4.describe('sayName', function () {
        it('Получаем фразу с новым именем', function () {
            assert.typeOf(sayName('Ivan'), 'string')
        });
    }); - написать проверку
    5.Запустить проверку mocha test
    6.Также их нужно подключить к html-страничке
    (Mocha css and js,Сhai js),через CDN облака
    7.Затем создать блок для отображения результата
    <div id="mocha">
    mocha.setup('bdd');
    
    let assert = chai.assert;
    </div>
    8.Затем нужно их настроить
    9.Затем нужно подключить скрипт с тестами
    10.После нужно запустить mocha
    
    <script>
     mocha.run();
    </script>
    
    Пример как их правильно подключать -
    <div id="mocha"></div>
    	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/mocha/9.0.0/mocha.min.css" integrity="sha512-6RWs5FczsXwODIZJoL0szz3bXX5W+q/m7bFVQ7HSwZbBXd7PpyYrg2f9Dr4Y2Guqi5k4NJsemxXuiiFoI8fQ4A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    	<script src="https://cdnjs.cloudflare.com/ajax/libs/mocha/9.0.0/mocha.min.js" integrity="sha512-HOhQd2fRBgb0MeoKSFSIh4CacvC8fvlby3y++1aCSQWE5AvmDFjZWSW/CmKwjQFMf+ciriEo6B6gcXvpqUfOww==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    	<script src="https://cdnjs.cloudflare.com/ajax/libs/chai/4.3.4/chai.min.js" integrity="sha512-gkZWobgJrQevN2HMEeTnSlxWPJ3HS0JJ3nXcgI6XLK/NI0z59jbztRZqbTlIzfl21vIGahQaeW0knwH1az/tbg==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    	<script>
    		mocha.setup('bdd');
    
    		let assert = chai.assert;
    	</script>
    	<script src="test/test.js"></script> - файл с тестом
    	<script src="js/script.js"></script> - основные файлы
    	<script src="./dist/bundle.js"></script> - основные файлы
    
    	<script>
    		mocha.run(); - запуск
    	</script>
    
    //mocha --wath - чтобы тесты делались по умолчанию сами
    //Ещё проверки можно посмотреть в документации Chai
    //browsersync - для овтаматической обновления брауз
    ```
    

# Глава 2

- **Настройка проекта**
    
    ```jsx
    npm i - чтобы установить все плагины и т.д. указаные в gulpfile.js(например)
    npx gulp - запуск gulp
    
    (это должно быть написано в package.json)
    "browserslist": [
        "> 1%",       -код в нашем проекте будет работать в браузерах выше 1% использования
        "not dead"
      ],
    
    продолжение 20:00
    ```
    
- ***Работа с серевером***
    
    ```jsx
                                     ***Ajax
       *Для начала нужно убрать стандартное поведение браузера для элемента*
        e.preventDefault(); - отмена стандартного поведения брвузера
    
    const formData = new FormData(item); - позволяет получить всю информацию из форм
    fetch() - этот метод позволяет отправлять запрос(крч типо новой версия XMLHttpRequest 
    и так далее)
    
    let res = await fetch(url, {
                method: 'POST',
                body: data
            }); - пример
    
    async,await - позволяют js показывать ,какие данные(функции) ассинхронные 
    и дожидаться запроса с сервера
    
    const postData = async (url, data) =>{ - показываем js,что эти арг-ты ассинхронные
            document.querySelector('.status').textContent = message.loading;
    
            let res = await fetch(url, { - показываем js,что нужно дождаться ответа 
                                            от сервера после отправки запроса
                method: 'POST', - пример
                body: data
            });
        }
    
    *Лучше метод fetch использовать при вызове функции(как в примере на вверху),так,как 
    можно будет не указывать постоянно какие данные отправлять в fetch,а просто менять их
    в арг-тах функции)****
    
    продолжение 15:30
    ```
    

[https://nisnom.com/](https://nisnom.com/) - сайт с готовыми решениями