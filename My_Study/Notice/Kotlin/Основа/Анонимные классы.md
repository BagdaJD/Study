```kotlin
fun main() {  
    travel(object : Transport("ABobus"){  
        override fun drive() {  
            println("Driving on Abobus")  
        }    })}  
  
fun travel(transport: Transport){  
    transport.drive()  
}
//Для интерфейсов также

//Немного про оптимизацию
//лучше это реализовать следующим образом

inline fun callVodonosa(bringWater: () -> Unit){  
    bringWater()  
}

//если убрать inline, то по сути будет происходить тоже самое, что и во время работы через анонимнй класс, т.е будет создавать какой-то объект класса, который будет реализовывать метод bringwater, а с инлайн будет выполняться только реализация

sportsmen.callVodonosa {println("Water")}
//лучше использовать inline функции вместо анонимных классов
```