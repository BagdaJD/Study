```kotlin
class country{
	var name: String
	var population: Long
	
	constructor(name: String, population: Long){
		this.name = name;
		this.population = population;
	}
}

//Это пример вторичного конструктора

class Country(var name: String, var population: Long)
//Это пример первичнго конструктора

//main.kt
val russia = Country("Russia", 144_000_000)
println(russia.name)

//Для того, чтобы поля(свойства) класса нельзя было изменять, пишем val name: String

//При объявлении вторичного конструктора мы обязаны вызвать в нем первичный +проинициализировать все поля класса

class Country(var name: String = " ", var population: Long = 0)
//эти строчки имеет один и тот же функционал
constructor(name: String, population: Long){
		this.name = " ";
		this.population = 0;
	}
//Добавляем вторичный конструктор, в случае, если ничего не ввели при создании объекта
constructor() : this("", 0)
//Аналогичная строка
```